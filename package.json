{
  "name": "control-diabetes-app",
  "version": "1.0.0",
  "description": "Sistema de Auditoria y Control de Diabetes e Insumos",
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install && cd ..",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && cd ..",
    "start": "cd backend && npm start"
  }
}
